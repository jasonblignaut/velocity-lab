{
  "name": "velocity-lab",
  "version": "1.0.0",
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241022.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.3"
  },
  "scripts": {
    "deploy": "wrangler deploy && wrangler pages deploy public"
  }
}